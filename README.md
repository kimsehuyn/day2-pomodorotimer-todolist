# Focus Flow — Pomodoro Timer & Todo List

HTML, CSS, JavaScript만으로 만든 포모도로 타이머와 할 일 목록 결합 앱입니다.

## 주요 기능

### 포모도로 타이머
- **집중 25분 · 짧은 휴식 5분 · 긴 휴식 15분** (4세션 후 긴 휴식)
- 아날로그 타이머 스타일 UI (0~55분 눈금, 빨간 웨지 진행 표시)
- 중앙 노브 버튼으로 시작 / 일시정지
- 세션 카운터 및 초기화
- 타이머 종료 시 알림음
- 할 일과 연동 — 선택한 작업에 포모도로 횟수 자동 기록

### 할 일 목록
- 할 일 추가, 완료, 삭제
- **전체 / 진행중 / 완료** 필터
- **별표(★)** 로 중요 할 일 표시
- **카테고리** (업무 · 공부 · 생활 · 개인) — 클릭 시 순환 변경
- 완료율 원형 진행률 표시
- `localStorage`에 자동 저장 (새로고침 후에도 유지)

## 디자인 참고

| 영역 | 참고 |
|------|------|
| 타이머 | 물리적 아날로그 포모도로 타이머 — 흰색 다이얼, 검은 눈금, 빨간 웨지 |
| 할 일 | [Todolize: To Do List & Planner](https://play.google.com/store/apps/details?id=todolist.scheduleplanner.dailyplanner.todo.reminders&hl=ko) — 밝은 카드 UI, 필터, 별표, 카테고리 |

## 프로젝트 구조

```
├── index.html   # 앱 마크업
├── styles.css   # 스타일 (타이머 + 할 일 UI)
├── app.js       # 타이머·할 일 로직
└── README.md
```

## 실행 방법

별도 빌드나 설치 없이 `index.html`을 브라우저에서 열면 됩니다.

```bash
# 선택: 로컬 서버로 실행 (Python 설치 시)
python -m http.server 8080
# http://localhost:8080 접속
```

## 개발 이력

### v1.0 — 초기 버전
- 포모도로 타이머 + 할 일 목록 기본 기능
- 원형 프로그레스 링, 다크 테마 UI
- 할 일 선택 시 현재 작업 표시, 포모도로 카운트 연동

### v2.0 — UI 개선 (현재)
- **타이머**: 아날로그 다이얼 UI로 전면 교체
  - SVG 기반 0~55분 눈금 및 숫자
  - 남은 시간을 보여주는 빨간 웨지(파이 조각) — 시간 경과에 따라 시계 방향으로 축소
  - 중앙 노브 형태의 시작/일시정지 버튼
  - 모드별 웨지 색상 (집중: 빨강, 휴식: 초록, 긴 휴식: 파랑)
- **할 일**: Todolize 스타일 라이트 테마 적용
  - 오늘의 할 일 헤더 + 완료율 미니 차트
  - 필터 탭, 별표, 카테고리 컬러 바
  - 둥근 체크박스 및 카드형 리스트

## 오류 수정 사항

| 항목 | 내용 |
|------|------|
| 다이얼 눈금 중복 | 1분 단위 눈금(tick)과 점(dot)이 겹쳐 보이던 문제 → tick만 사용하도록 정리 |
| 기존 데이터 호환 | `localStorage`에 저장된 이전 할 일 데이터에 `starred`, `category` 필드가 없을 때 기본값(`false`, `work`) 자동 적용 |
| 완료된 할 일 선택 | 완료 처리된 할 일이 현재 작업으로 남아 있던 경우 → 선택 해제 및 UI 동기화 |
| 모드 전환 중 타이머 | 타이머 실행 중 모드 탭 클릭 시 상태 꼬임 방지 → 실행 중에는 모드 변경 불가 |

## 기술 스택

- HTML5
- CSS3 (CSS Variables, Flexbox, Grid)
- Vanilla JavaScript (ES6+)
- SVG (아날로그 다이얼, 웨지, 진행률 링)
- Web Audio API (알림음)
- localStorage (데이터 영속화)

## 라이선스

MIT
